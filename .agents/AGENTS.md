# Workspace Rules

## Atomic Commits
You MUST make small, atomic git commits with descriptive messages for every milestone or fix you make during this and future tasks. After completing a unit of work (e.g. updating the database schema, implementing a feature, fixing a bug), you should commit the changes. When the whole user request is completed, you should push the changes using `git push`.
